import { ArrowUpRight } from 'lucide-react';
import { useSubmissions } from '../context/SubmissionContext';
import { EmptyState, ErrorState, GallerySkeleton } from './DataStates';
export default function SubmissionGallery(){const{submissions,loading,loadError,loadSubmissions,count}=useSubmissions();return <section id="submissions" className="submissions-section"><div className="gallery-heading"><div><span className="eyebrow">Submission gallery</span><h2>Projects ready for review</h2><p>Shared data stays consistent across every component.</p></div><span className="gallery-count">{count} {count===1?'submission':'submissions'}</span></div>{loading?<GallerySkeleton/>:loadError?<ErrorState message={loadError} onRetry={loadSubmissions}/>:submissions.length?<div className="project-grid">{submissions.map(item=><article key={item.id} className="project-card"><div className="card-image"><img src={item.imageUrl} alt={`${item.projectTitle} cover`}/><span>{item.category}</span></div><div className="card-body"><div className="card-meta"><span>{new Date(`${item.submissionDate}T00:00:00`).toLocaleDateString('en',{month:'short',day:'numeric',year:'numeric'})}</span><span>{item.fullName}</span></div><h3>{item.projectTitle}</h3><p>{item.description}</p><div className="card-footer"><span>{item.techStack}</span><a href={item.projectUrl} target="_blank" rel="noreferrer" aria-label={`Open ${item.projectTitle}`}><ArrowUpRight size={17}/></a></div></div></article>)}</div>:<EmptyState/>}</section>}