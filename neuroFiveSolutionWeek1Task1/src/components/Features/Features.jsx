import Container from '../common/Container/Container';import SectionHeading from '../common/SectionHeading/SectionHeading';import useScrollReveal from '../../hooks/useScrollReveal';import {features} from '../../utils/constants';import styles from './Features.module.css';
function Card({item}){const r=useScrollReveal();const Icon=item.icon;return <article ref={r.ref} className={`${styles.card} ${r.className}`}><div className={styles.icon}><Icon size={24}/></div><h3>{item.title}</h3><p>{item.description}</p></article>};export default function Features(){return <section id="features"><Container><SectionHeading eyebrow="Powerful by default" title="Everything you need to move faster" description="One intelligent workspace to understand performance, align your team, and make confident decisions."/><div className={styles.grid}>{features.map(x=><Card item={x} key={x.title}/>)}</div></Container></section>}
