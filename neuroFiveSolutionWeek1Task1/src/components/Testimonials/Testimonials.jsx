import {Quote} from 'lucide-react';import Container from '../common/Container/Container';import SectionHeading from '../common/SectionHeading/SectionHeading';import useScrollReveal from '../../hooks/useScrollReveal';import {testimonials} from '../../utils/constants';import styles from './Testimonials.module.css';
export default function Testimonials(){const r=useScrollReveal();return <section id="testimonials" className={styles.section}><Container><SectionHeading eyebrow="Loved by modern teams" title="Clearer data. Better decisions."/><div ref={r.ref} className={`${styles.grid} ${r.className}`}>{testimonials.map((x,i)=><article key={x.name}><Quote className={styles.quote} size={28}/><p>“{x.quote}”</p><footer><span className={`${styles.avatar} ${styles[`a${i}`]}`}>{x.initials}</span><div><strong>{x.name}</strong><small>{x.role}</small></div></footer></article>)}</div></Container></section>}
