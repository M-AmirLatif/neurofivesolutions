import Container from '../common/Container/Container';import Button from '../common/Button/Button';import useScrollReveal from '../../hooks/useScrollReveal';import styles from './CTA.module.css';
export default function CTA(){const r=useScrollReveal();return <section id="cta" className={styles.section}><Container><div ref={r.ref} className={`${styles.banner} ${r.className}`}><div className={styles.glow}/><span>YOUR DATA IS READY</span><h2>Ready to unlock AI-powered analytics?</h2><p>Join ambitious teams turning complex data into their clearest competitive advantage.</p><div><Button variant="light" icon>Start Free Trial</Button><Button variant="secondary" href="mailto:hello@novapulse.ai">Book Demo</Button></div></div></Container></section>}
