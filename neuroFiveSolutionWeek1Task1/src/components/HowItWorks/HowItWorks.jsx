import Container from '../common/Container/Container';import SectionHeading from '../common/SectionHeading/SectionHeading';import useScrollReveal from '../../hooks/useScrollReveal';import {steps} from '../../utils/constants';import styles from './HowItWorks.module.css';
export default function HowItWorks(){const r=useScrollReveal();return <section id="how-it-works" className={styles.section}><Container><SectionHeading eyebrow="Simple setup" title="From raw data to real action" description="Get meaningful answers without complicated implementation or another tool your team has to learn."/><div ref={r.ref} className={`${styles.steps} ${r.className}`}>{steps.map((x,i)=>{const Icon=x.icon;return <article key={x.title}><span className={styles.number}>0{i+1}</span><div className={styles.icon}><Icon/></div><h3>{x.title}</h3><p>{x.description}</p></article>})}</div></Container></section>}
