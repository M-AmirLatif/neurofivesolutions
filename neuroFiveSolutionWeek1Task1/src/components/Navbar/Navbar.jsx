import { useEffect,useState } from 'react';import { Menu,X,Activity } from 'lucide-react';import Container from '../common/Container/Container';import Button from '../common/Button/Button';import {navLinks} from '../../utils/constants';import styles from './Navbar.module.css';
export default function Navbar(){const[open,setOpen]=useState(false);const[scrolled,setScrolled]=useState(false);useEffect(()=>{const fn=()=>setScrolled(scrollY>16);addEventListener('scroll',fn,{passive:true});return()=>removeEventListener('scroll',fn)},[]);return <header className={`${styles.navbar} ${scrolled?styles.scrolled:''}`}><Container className={styles.inner}><a href="#top" className={styles.logo} aria-label="NovaPulse home"><span><Activity size={19}/></span>NovaPulse</a><nav className={`${styles.links} ${open?styles.open:''}`} aria-label="Main navigation">{navLinks.map(([label,id])=><a key={id} href={`#${id}`} onClick={()=>setOpen(false)}>{label}</a>)}<Button className={styles.mobileCta} href="#pricing">Start Free Trial</Button></nav><div className={styles.actions}><Button href="#pricing">Start Free Trial</Button><button className={styles.menu} onClick={()=>setOpen(!open)} aria-expanded={open} aria-label={open?'Close menu':'Open menu'}>{open?<X/>:<Menu/>}</button></div></Container></header>}
