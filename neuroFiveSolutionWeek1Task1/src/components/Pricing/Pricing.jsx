import {Check} from 'lucide-react';import Container from '../common/Container/Container';import SectionHeading from '../common/SectionHeading/SectionHeading';import Button from '../common/Button/Button';import useScrollReveal from '../../hooks/useScrollReveal';import {prices} from '../../utils/constants';import styles from './Pricing.module.css';
export default function Pricing(){const r=useScrollReveal();return <section id="pricing"><Container><SectionHeading eyebrow="Simple pricing" title="Choose the plan that fits" description="Start with everything you need today. Upgrade whenever your ambitions grow."/><div ref={r.ref} className={`${styles.grid} ${r.className}`}>{prices.map(p=><article key={p.name} className={p.popular?styles.popular:''}>{p.popular&&<span className={styles.badge}>Most Popular</span>}<h3>{p.name}</h3><p>{p.desc}</p><div className={styles.price}><strong>${p.price}</strong><span>/ month</span></div><Button variant={p.popular?'primary':'secondary'} href="#cta">Start Free Trial</Button><ul>{p.items.map(x=><li key={x}><Check size={16}/>{x}</li>)}</ul></article>)}</div></Container></section>}
